const express = require('express');
const router = express.Router();

// Placeholder endpoints (adjust to your schema later)
router.get('/departments', (_req, res) => res.json({ success: true, data: [] }));

module.exports = router;
