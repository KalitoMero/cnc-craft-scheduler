const express = require('express');
const router = express.Router();

// Placeholder endpoints (adjust to your schema later)
router.get('/additional-infos', (_req, res) => res.json({ success: true, data: [] }));

module.exports = router;
